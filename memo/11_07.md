スレッドの終了を待つ処理とmutexなどを解法する処理を書いて、コンパイルして簡単なテストをする(引数のテスト)

動的に割り当てた資源：
- mutex:
  - monitor
  - philosopher
  - fork
- thread:
  - monitor
  - philosopher

大体の処理は書けたから、あとは頭の中でテストしてみるか

INT32 に収まるか確認する方法を見て、今の方法が間違っていることに気づいた
strtol() を使うといいらしい

ft_strtol()を実装しよう.
10進数だけ対応すればいいか.

long strtol(const char *nptr, char **endptr, int base);

base == 0 で、数字の先頭が:
- 0x or 0X なら base = 16
- 0 なら base = 8
- otherwise base = 10

unsigned long を負の数にすると、ビットが反転して１を足す操作になる.
その後 long 型にキャストすると、ビットパターンで再解釈するから（c言語）、マイナスの数として解釈される

endptr if not NULL:
- if *endptr = \0, all digits are valid

return:
- if overflow, LONG_MAX
- if underflow, LONG_MIN
and set errno = ERANGE

## テストケース

### Invalid test case
./philo 5
./philo 3 2 1 9



