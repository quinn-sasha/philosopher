監視者のスレッド処理を書く
- 役割：シュミレーションが終了したかどうかを検出する
- 終了条件:
  - max_eat に達した（max_eat引数がある場合）
  - 誰か死んだ

誰かが死んでいた、しかし誰かは食事を終えいていた場合はどうなるんだろう
どっちが先かでシュミレーションが成功したかどうかかわりそうだけど

死んでいたら即終了
食べ終わっていても、まだ他の哲学者の状態を見ている

> number_of_times_each_philosopher_must_eat (optional argument): If all
philosophers have eaten at least number_of_times_each_philosopher_must_eat
times, the simulation stops. If not specified, the simulation stops when a
philosopher dies.

全員満腹にならなければいけなくて、誰か一人でも死んだらシュミレーション失敗
死亡ログを書くと同時に、monitor.is_dead = true にする

unsafe_is_dead()
死亡条件: now > last_eat_at + time_to_die
また死亡していたときにその時刻をもう一度取得するのは、実際の死亡時間よりずれてしまう.
だから is_dead()の引数から確認した時間を取得できるようにする

last_eat_at + time_to_die <- msec以下が切り捨てられているから、nowのmsec以下は切り捨てる
